# `@pi/core`

The provider-neutral agent loop, built-in coding tools, execution policy,
durable journal, context management, and JSONL sessions used by
[piko](https://github.com/arunmenon/piko).

This package is currently pre-1.0, POSIX-only, and intentionally private pending
a project license/release decision. Its API may change between minor releases.
