Run the release gate for version $ARGUMENTS.

Read .agent/skills/release-checklist.md and execute it in order. Run every machine-verifiable gate yourself and capture raw outputs into docs/releases/$ARGUMENTS/. STOP at the human gates and present the evidence pack with your pass/fail assessment for each gate — never sign off a human gate yourself.
