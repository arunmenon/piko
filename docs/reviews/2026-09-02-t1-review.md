# T1 re-review (received 2026-09-02)

Re-issue of docs/reviews/codex-review-prompt.md against the tree at fdc39bb
after maturity plan tranche 1. Received verbatim from the operator;
recorded as provenance. Weighted result 2.65/5, reported 2.7 (from 2.55).

Findings (all CONFIRMED): (1) descriptor-anchored containment open and
the 0022 evidence map omitted map traversal, directory creation, temp
placement, rename, cleanup; (2) host-bash credential separation
bypassable via parent-process inspection (ps eww $PPID), warning must say
so until 0018; (3) TB 2.0 manifest generator covers only a marked block
while the page claims it is the source for every count, and CI does not
check it; (4) per-turn corrections did not reach the committed r2 review
extracts or the matrix record; (5) SIGTERM still aborts (0027 correctly
proposed); (6) live telemetry rows and cursors under .project/telemetry
are tracked and dirty the tree during normal operation; (7) README,
CHANGELOG, and 0006 still overstate the containment boundary; (8) 0021's
redaction language conflicts with session journals retaining plaintext;
permanent-retention defaults ignore deletion obligations; (9)
check-budget.ts comment still cites 0017 for token rent; (10) changelog
says versions mark tagged states while no tag exists.

Scorecard: correctness 3, security 2, failure honesty 3, financial 3,
tests 3, docs 3 (up from 2), ops 2, benchmarks 2.

Closure recommendation: T1 stays open for one corrective T1.1 commit
(supersede stale extracts; make the single-source claim true and CI-
enforced; untrack live telemetry; expand the 0022 map). Then T1 closes at
2.7 and T2 remains gated on owner decisions.
